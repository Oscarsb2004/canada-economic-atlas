# registry/datasets/gdp-provincial-annual.yaml - one dataset the pipeline makes (docs/REBUILD.md section 3).
# `python -m atlas.run --dataset gdp-provincial-annual` builds it; atlas/core/registry.py checks every claim below.

title: Real GDP by industry and province, annual, chained 2017 dollars (36-10-0711)
group: economy
order: 70
builder: atlas.datasets.statcan_sectors:pull
params:
  pull: provincial_annual
sources:
- statcan_wds
shells:
- fetcher
- statcan_table
after: []
outputs:
- data/sectors/provincial-annual.json
frame:
  profile: panel
  record_type: observation
on_screen: true
consumed_by:
- path: pipeline/99_bundle.py
  how: copied into the site bundle, web/public/data/
