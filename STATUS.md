# STATUS — what this project makes

_Generated by `python run.py --status` from registry/. Do not edit by hand; `run.py --check` refuses a stale copy._

The plan for the restructure, and the only queue, is [`docs/REBUILD.md`](docs/REBUILD.md).
Why things are the way they are is in `docs/`; this file is only what exists.

## A full run

```bash
python run.py            # every step below, then verification
python run.py --check    # every registry file against its schema
```

| Step | Runs |
| --- | --- |
| 01 | `-m atlas.run projects` |
| 02 | `-m atlas.run economy` |
| 03 | `-m atlas.run business-counts` |
| 04 | `-m atlas.run corridors` |
| 05 | `-m atlas.run municipalities` |
| 06 | `-m atlas.run industries` |
| 07 | `-m atlas.run vessels` |
| 08 | `-m atlas.run provinces` |
| 99 | `-m atlas.run bundle` |

## Datasets

### bundle

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **bundle**<br>The bundle the site fetches: every dataset it shows, plus meta, country and palette | `web/public/data/events/major-projects-office/projects.json` (244 KB)<br>`web/public/data/events/major-projects-office/strategies.json` (32 KB)<br>`web/public/data/events/major-projects-office/industries.json` (52 KB)<br>`web/public/data/events/trade-corridors/corridors.json` (46 KB)<br>`web/public/data/sectors/national-monthly.json` (325 KB)<br>`web/public/data/sectors/national-constant.json` (325 KB)<br>`web/public/data/sectors/provincial-annual.json` (437 KB)<br>`web/public/data/sectors/rates.json` (1 KB)<br>`web/public/data/sectors/business-counts.json` (56 KB)<br>`web/public/data/provinces/provinces.json` (368 KB)<br>`web/public/data/country.json` (3 KB)<br>`web/public/data/palette.json` (3 KB)<br>`web/public/data/meta.json` (12 KB) | yes | `web/src/data/bundle.ts`<br>`docs/INTEROP-world-strategic-map.md` |

### business-counts

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **business-counts**<br>Canadian Business Counts, with employees, by industry, province and size (newest table) | `data/sectors/business-counts.json` (56 KB) | yes | `atlas/export/bundle.py` |

### corridors

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **trade-corridors**<br>Canada's five national trade corridors, with their ports and border crossings | `data/events/trade-corridors/corridors.json` (46 KB) | yes | `atlas/export/bundle.py` |

### economy

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **gdp-national-monthly**<br>Real GDP by industry, monthly, chained 2017 dollars (36-10-0434) | `data/sectors/national-monthly.json` (325 KB) | yes | `atlas/export/bundle.py` |
| **gdp-national-constant**<br>Real GDP by industry, monthly, 2017 constant prices (36-10-0434) | `data/sectors/national-constant.json` (325 KB) | yes | `atlas/export/bundle.py` |
| **gdp-national-annual-current**<br>GDP at basic prices by industry, annual, current dollars (36-10-0710) | `data/sectors/national-annual-current.json` (32 KB) | no | `registry/sectors.yaml` |
| **capex-annual**<br>Capital expenditures by industry and province, annual (34-10-0035) | `data/sectors/capex-annual.json` (407 KB) | no | `registry/sectors.yaml` |
| **employment-monthly**<br>Employment by industry (SEPH), monthly, unadjusted (14-10-0201) | `data/sectors/employment-monthly.json` (5.5 MB) | no | `registry/sectors.yaml` |
| **gross-output-annual**<br>Gross output by industry, annual, IOIC summed to sectors (36-10-0488) | `data/sectors/output-annual.json` (478 KB) | no | `registry/sectors.yaml` |
| **gdp-provincial-annual**<br>Real GDP by industry and province, annual, chained 2017 dollars (36-10-0711) | `data/sectors/provincial-annual.json` (437 KB) | yes | `atlas/export/bundle.py` |
| **statcan-cube-hashes**<br>The sha256 of every StatCan cube zip read, the change signal for the bundle | `data/sectors/_cubes.json` (1 KB) | no | `atlas/export/bundle.py` |
| **policy-rate**<br>Bank of Canada target for the overnight rate | `data/sectors/rates.json` (1 KB) | yes | `atlas/export/bundle.py` |

### industries

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **project-industries**<br>Each MPO project placed in NAICS, with NRCan's status and capital cost | `data/events/major-projects-office/industries.json` (52 KB) | yes | `atlas/export/bundle.py` |

### municipalities

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **census-subdivisions**<br>Every census subdivision, 2021, with population and dwelling counts (98-10-0002) | `data/geography/municipalities.json` (5.6 MB) | no | `registry/checks.yaml` |

### projects

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **major-projects**<br>The Major Projects Office portfolio: projects, strategies and what each source says exists | `data/events/major-projects-office/projects.json` (244 KB)<br>`data/events/major-projects-office/strategies.json` (32 KB)<br>`data/events/major-projects-office/coverage.json` (3 KB) | yes | `atlas/export/bundle.py`<br>`registry/checks.yaml` |

### provinces

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **province-profiles**<br>Every province and territory: finances, sector shares, budget passages and symbols | `data/provinces/provinces.json` (368 KB) | yes | `atlas/export/bundle.py` |

### vessels

| Dataset | Writes | On the site | Read by |
| --- | --- | --- | --- |
| **vessel-register**<br>The Canadian Register of Large Vessels: every entry carrying an IMO number | `data/vessels/large-vessel-register.json` (1.4 MB) | no | `live/collect_ais.py` |

## What they are built from

**17 shells** (`registry/shells/`): `absent_cells`, `ais_stream`, `arcgis_layer`, `commons_media`, `crosswalk_sum`, `document_text`, `fetcher`, `geo_distance`, `image_derive`, `join_distance`, `paired_values`, `partition_drift`, `site_crawl`, `statcan_table`, `valet_series`, `verbatim_quotes`, `workbook_edition`.

**21 sources** (`registry/sources/`): `aisstream`, `boc_valet`, `canada_placenames`, `finance_fiscal_reference_tables`, `geogratis_names`, `mpo_arcgis`, `mpo_pages`, `nrcan_mpi_2025`, `nrcan_nrwn`, `pch_provincial_symbols`, `provincial_budgets`, `statcan_boundaries`, `statcan_business_counts`, `statcan_gdp_shares_provincial`, `statcan_municipal_population`, `statcan_naics_2022`, `statcan_population_centres`, `statcan_wds`, `tc_annual_report`, `tc_large_vessel_register`, `wikimedia_commons`.

Each shell card states its contract, invariants, refusals and the benchmark that proves it; each
source card carries its licence, read and dated before its data was used.

## What proves it still works

```bash
python run.py --test                             # the test suite
python run.py --verify                           # independent verification of the published files
python verify/golden.py replay --name legacy-v1  # the restructure's identity check
```

The golden master is the tag `legacy-v1`: the project as it stood before the restructure, run once
with every download recorded. Any step of the restructure must reproduce its output byte for byte.
